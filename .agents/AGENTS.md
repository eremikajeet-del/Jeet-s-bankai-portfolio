
# Automatic GitHub Push Rule
Whenever you (the AI) make any code changes, or you are informed that the user made code changes, you MUST automatically run "git add .", "git commit -m 'Auto-update'", and "git push" to the remote repository before ending your turn. This ensures the user's GitHub contribution graph stays consistently updated.

